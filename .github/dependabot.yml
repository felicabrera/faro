# Dependabot version updates.
#
# Security updates are not configured here. They are already enabled at
# repository level (Dependabot alerts plus automated security fixes) and fire on
# their own whenever an advisory affects the project.
#
# Today the repository only contains GitHub Actions workflows, so that is the
# only active ecosystem. Declaring `gomod` or `npm` without their manifest
# present makes Dependabot fail on every run. Uncomment the blocks below once
# the code lands, and adjust `directory` if the Next.js audit frontend lives in
# a subdirectory such as `/web`.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    # Do not accept a brand new release the moment it is published. A
    # compromised package is usually caught within days, and this window lets
    # that happen before the update reaches a PR here. Security updates are
    # explicitly exempt from cooldown, so vulnerability fixes still arrive
    # immediately.
    #
    # Only `default-days` is valid for the github-actions ecosystem. The
    # `semver-*-days` keys are rejected here and are only accepted by
    # semver-aware ecosystems such as gomod and npm.
    cooldown:
      default-days: 7
    labels: ["dependencies", "ci"]
    commit-message:
      prefix: "chore(ci)"

  # - package-ecosystem: "gomod"
  #   directory: "/"
  #   schedule:
  #     interval: "weekly"
  #     day: "monday"
  #   open-pull-requests-limit: 10
  #   labels: ["dependencies", "go"]
  #   groups:
  #     go-minor-patch:
  #       update-types: ["minor", "patch"]
  #   commit-message:
  #     prefix: "chore(deps)"

  # - package-ecosystem: "npm"
  #   directory: "/web"
  #   schedule:
  #     interval: "weekly"
  #     day: "monday"
  #   open-pull-requests-limit: 10
  #   labels: ["dependencies", "javascript"]
  #   groups:
  #     npm-minor-patch:
  #       update-types: ["minor", "patch"]
  #   cooldown:
  #     default-days: 7
  #     semver-major-days: 30
  #   commit-message:
  #     prefix: "chore(deps)"
